%li.list-group-item
  .media
    .media-body
      %h4.media-heading
        Rating: #{review.rating}
      %em
        "#{review.content}"
      \-
      = succeed ',' do
        = link_to review.author, user_path(review.user)
      #{review.created_at.strftime("%d-%m-%Y")}
